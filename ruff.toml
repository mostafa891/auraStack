line-length = 100
target-version = "py313"

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
fixable = ["ALL"]

[lint.per-file-ignores]
# Ignore wildcard import warnings for split settings modules
"core/settings/*.py" = ["F403", "F405"]
"scripts/*.py" = ["E402"]